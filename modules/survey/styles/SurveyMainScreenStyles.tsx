import { StyleSheet } from "react-native";
import {METRICS} from "../../../constants/theme"

export const surveyMainScreenStyles = StyleSheet.create({
    mainContainer: {
        flex:1,
        alignItems:'center',
        justifyContent:'center',

    },


})